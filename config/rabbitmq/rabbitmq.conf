# RabbitMQ runtime settings
#
# Celery 顶刊追踪任务可能运行超过 30 分钟；RabbitMQ 3.12 默认 consumer_timeout=1800000ms
# 会关闭通道并导致 worker 报错：PRECONDITION_FAILED - delivery acknowledgement timed out.
#
# 这里将超时提高到 24 小时（毫秒）。
consumer_timeout = 86400000
